# 双系统设置默认启动的系统

方法：通过GRUB引导，在Linux系统进行修改

```bash
# 1. 编辑GRUB配置文件
sudo vim /etc/default/grub

# 2. 修改以下行，如果是字符串需要与系统的相匹配
GRUB_DEFAULT="Windows Boot Manager"
# 或如果Windows在菜单第n位（从0开始计数）
GRUB_DEFAULT=3

# 3.重新生成GRUB配置
sudo grub2-mkconfig -o /boot/grub2/grub.cfg

# 4.验证设置
sudo grub2-editenv list
```
